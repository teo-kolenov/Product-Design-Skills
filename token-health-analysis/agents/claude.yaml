interface:
  display_name: "Token Health Analysis"
  short_description: "Create token health dashboards from Figma links, JSON tokens, or text token sources"
  default_prompt: "/token-health-analysis HELP"

